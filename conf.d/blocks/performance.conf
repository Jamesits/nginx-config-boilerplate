gzip    on;
gzip_disable	"msie6";
gzip_proxied    any;
gzip_comp_level 2;
gzip_buffers    16  8k;
gzip_min_length 512;
gzip_proxied	expired no-cache no-store private auth;
gzip_types	text/plain text/css application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript application/javascript application/rss+xml application/atom+xml application/rdf+xml;

sendfile     on;
tcp_nopush   on;
tcp_nodelay  on;

http2 on;
# http2_push_preload on;
